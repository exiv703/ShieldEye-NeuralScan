# ShieldEye NeuralScan - Dependencies
# Python 3.10+ required

# ============================================================================
# GUI Framework (GTK 4)
# ============================================================================
# Note: PyGObject is provided by the OS package manager and inherited via
# `python3 -m venv --system-site-packages`. Building it from PyPI requires
# girepository / cairo dev libs and breaks on Python 3.14. Install on host:
#   Arch:   sudo pacman -S python-gobject gtk4
#   Debian: sudo apt install python3-gi gir1.2-gtk-4.0

# ============================================================================
# Data Visualization & Image Processing
# ============================================================================
# Note: Pillow and matplotlib are installed system-wide on Arch
# (python-pillow, python-matplotlib) and inherited via --system-site-packages.
# Pinned old versions don't have Python 3.14 wheels and fail to compile.
# Install on host if missing:
#   Arch:   sudo pacman -S python-pillow python-matplotlib
#   Debian: sudo apt install python3-pil python3-matplotlib

# ============================================================================
# AI/ML Stack - Code Analysis (heavy; pip will skip if wheels missing)
# ============================================================================
# Loose pins so pip can pick wheels that match the running Python version.
# If install fails (e.g. no cp314 wheel for torch), scanner falls back to
# heuristic-only analysis - GUI still runs.
transformers>=4.46
accelerate>=1.0
safetensors>=0.5
torch>=2.5

# ============================================================================
# Container Integration
# ============================================================================
docker>=7.1

# ============================================================================
# Additional Dependencies
# ============================================================================
jsonschema>=4.23
python-dateutil>=2.9
pytest>=8.3

# ============================================================================
# Development Tools (Optional - uncomment if needed)
# ============================================================================
# pylint==3.2.7
# black==24.8.0
# isort==5.13.2
# mypy==1.11.2
# pytest-cov==5.0.0
# pytest-mock==3.14.0
# sphinx==8.0.2
# myst-parser==4.0.0
# ipython==8.27.0
# pdbpp==0.10.3
